js
export défaut function
handler(req,res){return: statuts(200).json({success:true, message :true
                                            message : deposit Api actve.});
js
export défaut function
handler(req,res)} returnn: statuts (200).json({success:true, message : withdrawale.Api active})
                                              }
